import { Button } from '..';

const Form = () => {
  return (
    <div>
      <Button variant='Destructive' className='bg-destructive/50'>
        Hello
      </Button>
    </div>
  );
};

export default Form;
